#include "platal_map.h"

Map::Map() {}

void Map::DrawMap() {}


// void Map::AddObject(Object item){}
// to try later, still figuring out how to use std::map

/* *Object Map::RemoveObject(int obj_id){
    for(std::map<(int,int),Object>::iterator i = *(objects_).begin();i != *(objects_).end();i++){
        if (*(objects_[i])==obj_id){
            *(objects_).remove(i);
            return obj_id
        }
    }

}*/
